-- =============================================================================
-- Файл: upd_db.sql
-- Назначение: последовательные обновления схемы базы данных между релизами.
--             Каждый блок снабжайте номером версии, датой и кратким описанием.
-- Выполнение: от пользователя с правами на изменение схемы (часто postgres),
--             подключение к базе chat_cursor_auto:
--   psql -h localhost -U postgres -d chat_cursor_auto -v ON_ERROR_STOP=1 \
--     -f back/sql/upd_db.sql
-- На «чистой» установке достаточно init_db.sql; этот файл — для миграций.
-- =============================================================================

\set ON_ERROR_STOP on

-- -----------------------------------------------------------------------------
-- Версия: 2026-04-17 / 001
-- Описание: начальный файл миграций. Схема из init_db.sql считается базовой.
--             Добавляйте ниже новые ALTER / CREATE с комментариями.
-- -----------------------------------------------------------------------------

-- Пример будущей миграции (закомментировано):
-- ALTER TABLE messages ADD COLUMN IF NOT EXISTS edited_at TIMESTAMPTZ;
-- COMMENT ON COLUMN messages.edited_at IS 'Время последнего редактирования.';
