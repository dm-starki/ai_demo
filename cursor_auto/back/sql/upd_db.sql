-- =============================================================================
-- Файл: upd_db.sql
-- Назначение: последовательные обновления схемы БД после первоначальной инициализации.
-- Порядок: каждое изменение — отдельный блок с датой и описанием; применять по порядку.
-- Текущее состояние: после init_db.sql изменений не вносилось (заглушка для CI/релизов).
-- =============================================================================

-- -----------------------------------------------------------------------------
-- [2026-04-16] Версия 0 — заглушка
-- Описание изменений: нет операций; файл зафиксирован для ведения истории миграций.
-- При добавлении миграций описывать: цель, затронутые таблицы, обратимость, откат.
-- -----------------------------------------------------------------------------

-- Пример будущего блока (не выполнять как есть):
-- ALTER TABLE ...;
-- COMMENT ON COLUMN ... IS '...';
