{
  "name": "cursor-auto-chat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"yarn --cwd back dev\" \"yarn --cwd front dev\"",
    "build": "yarn --cwd back build && yarn --cwd front build",
    "db:init": "yarn --cwd back db:init",
    "clean": "node scripts/clean.mjs",
    "stop": "node scripts/stop.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
